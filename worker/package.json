{
    "name": "bein-worker",
    "version": "1.0.0",
    "description": "beIN Panel Automation Worker - Playwright-based queue processor",
    "main": "dist/index.js",
    "scripts": {
        "build": "mkdir -p prisma && cp ../prisma/schema.prisma prisma/schema.prisma && prisma generate && tsc",
        "start": "node dist/index.js",
        "dev": "ts-node src/index.ts",
        "install:browser": "npx playwright install chromium"
    },
    "keywords": [
        "bein",
        "automation",
        "playwright",
        "worker"
    ],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@prisma/client": "^7.2.0",
        "bullmq": "^5.34.10",
        "dotenv": "^16.4.7",
        "ioredis": "^5.4.2",
        "otplib": "^12.0.1",
        "playwright": "^1.50.1"
    },
    "devDependencies": {
        "@types/node": "^22.10.7",
        "prisma": "^7.2.0",
        "typescript": "^5.7.3",
        "ts-node": "^10.9.2"
    }
}